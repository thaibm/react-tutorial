import React from 'react';
import PropTypes from 'prop-types';

TodoList.propTypes = {
    
};

function TodoList() {
    return (
        <div>
            List Todo
        </div>
    );
}

export default TodoList;